[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm install", "npx prisma migrate deploy", "npx prisma generate", "npm run db:seed:core", "npm run build"]

[start]
cmd = "npm run start"
